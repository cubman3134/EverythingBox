{
  "build":    ["cmake", "--build", "build", "--config", "Release", "--target", "probe_ports", "--parallel"],
  "test":     ["build/Release/probe_ports.exe"],
  "artifact": "build/Release/probe_ports.exe",
  "sentinel": "PORTS-OK",
  "mutants": [
    { "name": "match-ignores-the-platform",
      "file": "native/src/core/NativePorts.h",
      "find": "        if (port.port.platform.compare(systemId.trimmed(), Qt::CaseInsensitive) != 0) return false;",
      "replace": "        if (false) return false;",
      "count": 1,
      "expect": "killed" },

    { "name": "match-ignores-the-title",
      "file": "native/src/core/NativePorts.h",
      "find": "        if (!titleKeys(port).contains(id.key)) return false;",
      "replace": "        if (titleKeys(port).isEmpty()) return false;",
      "count": 1,
      "expect": "killed" },

    { "name": "title-match-is-a-prefix-not-an-equality",
      "file": "native/src/core/NativePorts.h",
      "find": "        if (!titleKeys(port).contains(id.key)) return false;",
      "replace": "        { bool hit = false; for (const QString& k : titleKeys(port)) if (k.startsWith(id.key.left(12))) hit = true; if (!hit) return false; }",
      "count": 1,
      "expect": "killed" },

    { "name": "region-refusal-does-nothing",
      "file": "native/src/core/NativePorts.h",
      "find": "            if (!ok) return false;",
      "replace": "            if (false) return false;",
      "count": 1,
      "expect": "killed" },

    { "name": "revision-refusal-does-nothing",
      "file": "native/src/core/NativePorts.h",
      "find": "        if (!revisions.isEmpty() && !id.revision.isEmpty() && !revisions.contains(id.revision)) return false;",
      "replace": "        if (false) return false;",
      "count": 1,
      "expect": "killed" },

    { "name": "a-non-port-entry-matches-too",
      "file": "native/src/core/EmulatorRegistry.h",
      "find": "    bool isNativePort() const { return !port.name.isEmpty(); }",
      "replace": "    bool isNativePort() const { return true; }",
      "count": 1,
      "expect": "killed" },

    { "name": "the-basename-hints-are-not-match-candidates",
      "file": "native/src/core/NativePorts.h",
      "find": "        for (const QString& f : port.port.filenames)\n        {\n            const QString k = normalizeTitle(f);\n            if (!k.isEmpty() && !keys.contains(k)) keys << k;\n        }",
      "replace": "        (void)0;",
      "count": 1,
      "expect": "killed" },

    { "name": "the-region-gate-is-not-derived-from-the-basenames",
      "file": "native/src/core/NativePorts.h",
      "find": "        for (const QString& f : port.port.filenames)\n            for (const QString& r : identify(f).regions)\n                if (!out.contains(r)) out << r;",
      "replace": "        (void)port;",
      "count": 1,
      "expect": "killed" },

    { "name": "normalize-keeps-the-leading-article",
      "file": "native/src/core/NativePorts.h",
      "find": "        s.replace(leading, QString());",
      "replace": "        (void)leading;",
      "count": 1,
      "expect": "killed" },

    { "name": "normalize-keeps-the-no-intro-inversion",
      "file": "native/src/core/NativePorts.h",
      "find": "        s.replace(inverted, QString());",
      "replace": "        (void)inverted;",
      "count": 1,
      "expect": "killed" },

    { "name": "normalize-keeps-the-dump-metadata",
      "file": "native/src/core/NativePorts.h",
      "find": "        if (cut >= 0) s = s.left(cut);",
      "replace": "        (void)cut;",
      "count": 1,
      "expect": "killed" },

    { "name": "extension-strip-has-no-length-bound",
      "file": "native/src/core/NativePorts.h",
      "find": "        if (ext.isEmpty() || ext.size() > 4) return name;",
      "replace": "        if (ext.isEmpty()) return name;",
      "count": 1,
      "expect": "killed" },

    { "name": "extension-strip-accepts-a-non-alphanumeric-tail",
      "file": "native/src/core/NativePorts.h",
      "find": "        for (const QChar c : ext) if (!c.isLetterOrNumber()) return name;",
      "replace": "        for (const QChar c : ext) if (false) return name;",
      "count": 1,
      "expect": "killed" },

    { "name": "a-language-list-reads-as-a-region",
      "file": "native/src/core/NativePorts.h",
      "find": "        if (t == QStringLiteral(\"world\") || t == QStringLiteral(\"w\")) return QStringLiteral(\"world\");",
      "replace": "        if (!t.isEmpty()) return t;",
      "count": 1,
      "expect": "killed" },

    { "name": "every-rom-delivery-mode-claims-to-be-supported",
      "file": "native/src/core/NativePorts.h",
      "find": "    inline bool romDeliverySupported(const QString& mode) { return mode == QStringLiteral(\"in_app_menu\"); }",
      "replace": "    inline bool romDeliverySupported(const QString& mode) { return !mode.isNull() || true; }",
      "count": 1,
      "expect": "killed" },

    { "name": "asset-glob-marker-keeps-the-wildcards",
      "file": "native/src/core/NativePorts.h",
      "find": "        for (const QString& seg : glob.split(QLatin1Char('*'), Qt::SkipEmptyParts))",
      "replace": "        for (const QString& seg : QStringList{ glob })",
      "count": 1,
      "expect": "killed" },

    { "name": "the-port-is-credited-by-the-repo-not-its-owner",
      "file": "native/src/core/NativePorts.h",
      "find": "        return slash > 0 ? releaseRepo.left(slash) : releaseRepo;",
      "replace": "        return releaseRepo;",
      "count": 1,
      "expect": "killed" },

    { "name": "a-catalog-file-can-give-a-port-a-system-binding",
      "file": "native/src/core/NativePorts.h",
      "find": "        e.systems.clear();",
      "replace": "        if (o.contains(QStringLiteral(\"systems\"))) e.systems = EmulatorRegistry::jsonStrList(o.value(QStringLiteral(\"systems\")), true);",
      "count": 1,
      "expect": "killed" },

    { "name": "rom-extensions-keep-their-leading-dot",
      "file": "native/src/core/NativePorts.h",
      "find": "            while (x.startsWith(QLatin1Char('.'))) x.remove(0, 1);",
      "replace": "            (void)0;",
      "count": 1,
      "expect": "killed" },

    { "name": "releases-fallback-fires-on-any-url",
      "file": "native/src/core/EmulatorRegistry.h",
      "find": "        if (!u.endsWith(QStringLiteral(\"/releases/latest\"), Qt::CaseInsensitive)) return QString();",
      "replace": "        if (false) return QString();",
      "count": 1,
      "expect": "killed" },

    { "name": "newest-release-takes-a-draft",
      "file": "native/src/core/EmulatorRegistry.h",
      "find": "            if (o.value(QStringLiteral(\"draft\")).toBool()) continue;",
      "replace": "            if (false) continue;",
      "count": 1,
      "expect": "killed" },

    { "name": "the-shipped-catalog-is-not-embedded",
      "file": "native/resources/ports.qrc",
      "find": "<file alias=\"n64recomp.json\">../ports/n64recomp.json</file>",
      "replace": "<file alias=\"not-the-name-the-code-asks-for.json\">../ports/n64recomp.json</file>",
      "count": 1,
      "expect": "killed" }
  ]
}
