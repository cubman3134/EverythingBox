{
  "build":    ["cmake", "--build", "build", "--config", "Release", "--target", "probe_ps3update", "--parallel"],
  "test":     ["build/Release/probe_ps3update.exe"],
  "artifact": "build/Release/probe_ps3update.exe",
  "sentinel": "PS3UPDATE-OK",
  "env":      { "QT_QPA_PLATFORM": "offscreen" },
  "mutants": [
    { "name": "sha1-footer-not-excluded",
      "file": "native/src/core/ps3/Ps3UpdateInstaller.cpp",
      "find": "    qint64 remaining = f.size() > 0x20 ? f.size() - 0x20 : f.size();",
      "replace": "    qint64 remaining = f.size(); // mutated: whole-file hash (the pre-fix behavior)",
      "count": 1, "expect": "killed" },

    { "name": "version-fallback-dropped",
      "file": "native/src/core/ps3/Ps3InstalledVersion.cpp",
      "find": "    return Ps3Sfo::stringValue(sfo, \"VERSION\");",
      "replace": "    return std::nullopt; // mutated: APP_VER-only",
      "count": 1, "expect": "killed" },

    { "name": "appver-not-preferred",
      "file": "native/src/core/ps3/Ps3InstalledVersion.cpp",
      "find": "    if (auto app = Ps3Sfo::stringValue(sfo, \"APP_VER\")) return app;",
      "replace": "    { /* mutated: APP_VER ignored, VERSION wins */ }",
      "count": 1, "expect": "killed" },

    { "name": "kill-verify-ignores-fingerprint",
      "file": "native/src/core/ps3/Ps3InstalledVersion.cpp",
      "find": "    const std::optional<QByteArray> now = dirFingerprint(gameDir, abort);\n    return now && *now == *lastPrint;",
      "replace": "    return true; // mutated: version-at-target alone is trusted",
      "count": 1, "expect": "killed" },

    { "name": "kill-verify-skips-version",
      "file": "native/src/core/ps3/Ps3InstalledVersion.cpp",
      "find": "    if (!reachedTarget(gameDir, targetVersion)) return false;",
      "replace": "    { /* mutated: version check skipped */ }",
      "count": 1, "expect": "killed" },

    { "name": "verify-size-check-dropped",
      "file": "native/src/core/ps3/Ps3Pkg.cpp",
      "find": "        if (sz == e.size) continue;",
      "replace": "        continue; // mutated: any existing file passes",
      "count": 1, "expect": "killed" },

    { "name": "verify-existence-dropped",
      "file": "native/src/core/ps3/Ps3Pkg.cpp",
      "find": "        if (!fi.isFile()) return false;",
      "replace": "        if (!fi.isFile()) continue; // mutated: missing files pass",
      "count": 1, "expect": "killed" },

    { "name": "verify-zero-byte-accepted",
      "file": "native/src/core/ps3/Ps3Pkg.cpp",
      "find": "        if (!e.overwrite && sz > 0) continue;",
      "replace": "        if (!e.overwrite) continue; // mutated: 0-byte non-overwrite passes",
      "count": 1, "expect": "killed" },

    { "name": "verify-dirs-ignored",
      "file": "native/src/core/ps3/Ps3Pkg.cpp",
      "find": "            if (!fi.isDir()) return false;",
      "replace": "            { } // mutated: missing dirs pass",
      "count": 1, "expect": "killed" },

    { "name": "verify-dir-check-weakened",
      "file": "native/src/core/ps3/Ps3Pkg.cpp",
      "find": "            if (!fi.isDir()) return false;",
      "replace": "            if (!fi.exists()) return false; // mutated: a FILE at a dir entry's path passes",
      "count": 1, "expect": "killed" },

    { "name": "verify-file-check-weakened",
      "file": "native/src/core/ps3/Ps3Pkg.cpp",
      "find": "        if (!fi.isFile()) return false;",
      "replace": "        if (!fi.exists()) return false; // mutated: a DIR at a file entry's path passes",
      "count": 1, "expect": "killed" },

    { "name": "verify-sdat-branch-neutered",
      "file": "native/src/core/ps3/Ps3Pkg.cpp",
      "find": "        if (low == 0x09) { if (e.size == 0 ? sz == 0 : sz > 0) continue; return false; }",
      "replace": "        if (low == 0xFF) { if (e.size == 0 ? sz == 0 : sz > 0) continue; return false; }",
      "count": 1, "expect": "killed" },

    { "name": "verify-written-type-skip-neutered",
      "file": "native/src/core/ps3/Ps3Pkg.cpp",
      "find": "            continue; // RPCS3's switch DEFAULT skips it",
      "replace": "            { } // mutated: a type RPCS3 never writes is required anyway --",
      "count": 1, "expect": "killed" },

    { "name": "crypt-counter-not-positioned",
      "file": "native/src/core/ps3/Ps3Pkg.cpp",
      "find": "    quint64 carry = quint64(blockOffset);",
      "replace": "    quint64 carry = 0; // mutated: blockOffset ignored",
      "count": 1, "expect": "killed" },

    { "name": "entries-name-sanity-dropped",
      "file": "native/src/core/ps3/Ps3Pkg.cpp",
      "find": "        if (name.isEmpty() || name.startsWith('/') || name.contains('\\\\')) return std::nullopt;",
      "replace": "        { } // mutated: any name accepted",
      "count": 1, "expect": "killed" },

    { "name": "haszero-always-clean",
      "file": "native/src/core/ps3/Ps3InstalledVersion.cpp",
      "find": "        if (it.fileInfo().size() == 0) return true;",
      "replace": "        ; // mutated: 0-byte files invisible",
      "count": 1, "expect": "killed" },

    { "name": "coordinator-heal-seam-ignored",
      "file": "native/src/core/ps3/Ps3UpdateCoordinator.cpp",
      "find": "    if (!state_->needsUpdate(*titleId, latest) && (!intact_ || intact_(*titleId))) return false;",
      "replace": "    if (!state_->needsUpdate(*titleId, latest)) return false; // mutated: state trusted blindly",
      "count": 1, "expect": "killed" },

    { "name": "verify-backoff-never-suppresses",
      "file": "native/src/core/ps3/Ps3VerifyBackoff.cpp",
      "find": "    return markerInfo.exists() && age >= 0 && age < kRetryBackoffSecs;",
      "replace": "    return false; // mutated: backoff never suppresses",
      "count": 1, "expect": "killed" },

    { "name": "verify-backoff-never-expires",
      "file": "native/src/core/ps3/Ps3VerifyBackoff.cpp",
      "find": "    return markerInfo.exists() && age >= 0 && age < kRetryBackoffSecs;",
      "replace": "    return markerInfo.exists() && age >= 0; // mutated: a marker suppresses forever",
      "count": 1, "expect": "killed" },

    { "name": "verify-backoff-future-marker-fresh",
      "file": "native/src/core/ps3/Ps3VerifyBackoff.cpp",
      "find": "    return markerInfo.exists() && age >= 0 && age < kRetryBackoffSecs;",
      "replace": "    return markerInfo.exists() && age < kRetryBackoffSecs; // mutated: a future-dated marker reads fresh",
      "count": 1, "expect": "killed" },

    { "name": "verify-backoff-record-neutered",
      "file": "native/src/core/ps3/Ps3VerifyBackoff.cpp",
      "find": "    if (f.open(QIODevice::WriteOnly | QIODevice::Truncate))",
      "replace": "    if (false && f.open(QIODevice::WriteOnly | QIODevice::Truncate)) // mutated: no marker written",
      "count": 1, "expect": "killed" },

    { "name": "coordinator-suppression-ignored",
      "file": "native/src/core/ps3/Ps3UpdateCoordinator.cpp",
      "find": "    if (suppressed_ && suppressed_(*titleId)) return false;",
      "replace": "    { } // mutated: suppression ignored",
      "count": 1, "expect": "killed" }
  ]
}
