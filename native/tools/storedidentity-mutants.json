{
  "build":    ["cmake", "--build", "build", "--config", "Release", "--target", "probe_cloudmerge", "--parallel"],
  "test":     ["build/Release/probe_cloudmerge.exe"],
  "artifact": "build/Release/probe_cloudmerge.exe",
  "sentinel": "CLOUDMERGE-OK",
  "env":      { "QT_QPA_PLATFORM": "offscreen", "EB_ISOLATED_DATA_DIR": "1" },
  "mutants": [

    { "name": "DROPS-an-unmappable-row-instead-of-keeping-it",
      "file": "native/src/core/StoredIdentity.cpp",
      "find": "        if (!id.isEmpty()) seen.push_back(id);\n        out.append(e);",
      "replace": "        if (!id.isEmpty()) seen.push_back(id);\n        if (id.contains(QStringLiteral(\"://\"))) { changed = true; continue; }\n        out.append(e);",
      "count": 1,
      "expect": "killed" },

    { "name": "DROPS-unmappable-id-mapped-to-empty",
      "file": "native/src/core/StoredIdentity.cpp",
      "find": "    return StoredUrl::identity(playPath);\n}",
      "replace": "    return QString();\n}",
      "count": 1,
      "expect": "killed" },

    { "name": "DROPS-identity-takes-locations-rule-and-collapses-the-playlist",
      "file": "native/src/core/StoredUrl.h",
      "find": "inline QString identity(const QString& s) { return withoutCredentialParams(s); }",
      "replace": "inline QString identity(const QString& s) { return location(s); }",
      "count": 1,
      "expect": "killed" },

    { "name": "DROPS-two-colliding-entries-kept-as-two-rows",
      "file": "native/src/core/StoredIdentity.cpp",
      "find": "        if (!id.isEmpty() && seen.contains(id)) { changed = true; continue; }",
      "replace": "        if (false && !id.isEmpty() && seen.contains(id)) { changed = true; continue; }",
      "count": 1,
      "expect": "killed" },

    { "name": "DROPS-the-merge-clock-bumped-so-a-clean-copy-outranks-a-newer-edit",
      "file": "native/src/core/StoredIdentity.cpp",
      "find": "            p.insert(QStringLiteral(\"items\"), swept);",
      "replace": "            p.insert(QStringLiteral(\"items\"), swept);\n            p.insert(QStringLiteral(\"updatedAt\"), 0.0);",
      "count": 1,
      "expect": "killed" },

    { "name": "DROPS-a-peers-livetv-favourite-takes-the-local-one-with-it",
      "file": "native/src/core/CloudMerge.cpp",
      "find": "        ingest(QJsonDocument::fromJson(store().value(favKey(p)).toString().toUtf8()).array());\n        // The remote half is filtered on the way IN",
      "replace": "        ingest(withoutLiveTv(QJsonDocument::fromJson(store().value(favKey(p)).toString().toUtf8()).array(), QStringLiteral(\"itemId\")));\n        // The remote half is filtered on the way IN",
      "count": 1,
      "expect": "killed" },

    { "name": "DROPS-the-livetv-filter-eats-every-favourite",
      "file": "native/src/core/CloudMerge.cpp",
      "find": "bool isLiveTvId(const QString& id) { return id.startsWith(QStringLiteral(\"livetv:\")); }",
      "replace": "bool isLiveTvId(const QString& id) { return true || id.startsWith(QStringLiteral(\"livetv:\")); }",
      "count": 1,
      "expect": "killed" },

    { "name": "DROPS-the-sweep-only-touches-the-active-profile",
      "file": "native/src/core/StoredIdentity.cpp",
      "find": "        const QVector<QPair<QString, QString>> mine = profile == active\n                                                          ? roots : QVector<QPair<QString, QString>>();",
      "replace": "        if (profile != active) continue;\n        const QVector<QPair<QString, QString>> mine = roots;",
      "count": 1,
      "expect": "killed" },

    { "name": "MOVES-nothing-the-sweep-never-writes",
      "file": "native/src/core/StoredIdentity.cpp",
      "find": "        if (nId != itemId || nPath != path)",
      "replace": "        if (false && (nId != itemId || nPath != path))",
      "count": 1,
      "expect": "killed" },

    { "name": "MOVES-nothing-resolve-returns-the-play-path-verbatim",
      "file": "native/src/core/StoredIdentity.cpp",
      "find": "    if (playPath.isEmpty()) return playPath;",
      "replace": "    return playPath;",
      "count": 1,
      "expect": "killed" },

    { "name": "MOVES-nothing-identity-keeps-the-credential",
      "file": "native/src/core/StoredUrl.h",
      "find": "inline QString identity(const QString& s) { return withoutCredentialParams(s); }",
      "replace": "inline QString identity(const QString& s) { return s; }",
      "count": 1,
      "expect": "killed" },

    { "name": "MOVES-nothing-the-reader-never-cuts-the-query-so-no-url-is-recognised",
      "file": "native/src/core/Subsonic.cpp",
      "find": "    int end = url.size();\n    for (int i = 0; i < url.size(); ++i)\n        if (url.at(i) == QLatin1Char('?') || url.at(i) == QLatin1Char('#')) { end = i; break; }",
      "replace": "    const int end = url.size();",
      "count": 1,
      "expect": "killed" },

    { "name": "MOVES-the-wrong-thing-the-reader-matches-a-root-by-prefix",
      "file": "native/src/core/Subsonic.cpp",
      "find": "        if (!s.second.isEmpty() && s.second == root) return qualify(s.first, Kind::Track, id);",
      "replace": "        if (!s.second.isEmpty() && root.startsWith(s.second)) return qualify(s.first, Kind::Track, id);",
      "count": 1,
      "expect": "killed" },

    { "name": "MOVES-the-wrong-thing-any-endpoint-counts-as-a-stream",
      "file": "native/src/core/Subsonic.cpp",
      "find": "    if (!before.endsWith(tail)) return QString();\n    const QString root = before.left(before.size() - tail.size());",
      "replace": "    const QString root = before.endsWith(tail) ? before.left(before.size() - tail.size()) : before;",
      "count": 1,
      "expect": "killed" },

    { "name": "MOVES-only-the-itemId-and-leaves-the-path-tokenised",
      "file": "native/src/core/StoredIdentity.cpp",
      "find": "        const QString nPath = path.isEmpty()   ? path   : StoredIdentity::resolve(path,   QString(), roots);",
      "replace": "        const QString nPath = path;",
      "count": 1,
      "expect": "killed" },

    { "name": "MOVES-once-the-sweep-is-a-one-shot-after-all",
      "file": "native/src/core/StoredIdentity.cpp",
      "find": "    const QStringList keys = store().allKeys();\n    if (keys.isEmpty()) return false;",
      "replace": "    static bool done = false;\n    if (done) return false;\n    done = true;\n    const QStringList keys = store().allKeys();\n    if (keys.isEmpty()) return false;",
      "count": 1,
      "expect": "killed" },

    { "name": "MOVES-nothing-the-hint-is-never-consulted",
      "file": "native/src/core/StoredIdentity.cpp",
      "find": "    if (!indexHint.isEmpty() && indexHint != playPath) return StoredUrl::identity(indexHint);",
      "replace": "    if (false) return StoredUrl::identity(indexHint);",
      "count": 1,
      "expect": "killed" },

    { "name": "MOVES-the-wrong-thing-a-hint-is-trusted-verbatim",
      "file": "native/src/core/StoredIdentity.cpp",
      "find": "    if (!indexHint.isEmpty() && indexHint != playPath) return StoredUrl::identity(indexHint);",
      "replace": "    if (!indexHint.isEmpty() && indexHint != playPath) return indexHint;",
      "count": 1,
      "expect": "killed" },

    { "name": "LEAKS-a-peers-tokenised-playlist-is-not-cleaned-on-the-way-in",
      "file": "native/src/core/CloudMerge.cpp",
      "find": "    StoredIdentity::sweepPlaylists();\n}",
      "replace": "    (void)0;\n}",
      "count": 1,
      "expect": "killed" },

    { "name": "LEAKS-livetv-favourites-still-serialized",
      "file": "native/src/core/CloudMerge.cpp",
      "find": "    for (const QJsonValue& v : in)\n        if (!isLiveTvId(v.toObject().value(idField).toString())) out.append(v);",
      "replace": "    for (const QJsonValue& v : in)\n        if (true || !isLiveTvId(v.toObject().value(idField).toString())) out.append(v);",
      "count": 1,
      "expect": "killed" },

    { "name": "LEAKS-the-livetv-tombstone-still-carries-the-url-out",
      "file": "native/src/core/CloudMerge.cpp",
      "find": "        const QJsonArray tombs = withoutLiveTv(tombsToArray(favTombStore(p)), QStringLiteral(\"key\"));",
      "replace": "        const QJsonArray tombs = tombsToArray(favTombStore(p));",
      "count": 1,
      "expect": "killed" },

    { "name": "LEAKS-a-peers-livetv-favourite-is-accepted",
      "file": "native/src/core/CloudMerge.cpp",
      "find": "        ingest(withoutLiveTv(po.value(QStringLiteral(\"items\")).toArray(), QStringLiteral(\"itemId\")));",
      "replace": "        ingest(po.value(QStringLiteral(\"items\")).toArray());",
      "count": 1,
      "expect": "killed" }
  ]
}
