// Persistence for the learned segment tier: the intro/credits range a user marked once for a show+season,
// reused for every later episode of that season. Filled in by Task 2 — this stub exists so the app target
// keeps linking while MediaSegments (Task 1) lands first.
#pragma once
#include "MediaSegments.h"

#include <QString>
#include <QVector>
