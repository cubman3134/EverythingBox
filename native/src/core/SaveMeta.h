// Per-save metadata (hash/mtime/device/tombstone bookkeeping) backing the save-sync baseline.
// Stub: filled in by Task 4 of the trustworthy-save-sync track. It exists now only so the app target has a
// stable source list across the tasks.
#pragma once
