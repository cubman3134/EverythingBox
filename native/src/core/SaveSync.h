// Per-file emulator save sync — the transport that carries out a SaveSyncPlan::plan().
// Stub: filled in by Task 2 of the trustworthy-save-sync track. It exists now only so the app target has a
// stable source list across the tasks.
#pragma once
